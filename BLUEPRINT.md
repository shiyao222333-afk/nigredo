# Nigredo · 馏析 — 项目蓝图

> v1.1 · 2026-07-15 · B站结构化中转稳定；企微列为下一阶段

---

## 在 OpusMagnum 五器工坊中的角色

**Nigredo（馏析）= 外部数据采集引擎。**

分两大块：第一层是数据采集，第二层是采集数据处理。采集先做。

```
外部世界 → Nigredo(采集) → Citrinitas(知识存储) → Rubedo(赚钱执行)
              ↑                                         │
              └────── Rubedo 调用 Nigredo API ──────────┘
```

---

## 愿景

做一套能卖的采集工具。一人公司先用它养活自己，成熟后产品化。

---

## 当前重心

**B站视频摄入流程——结构化中转文件生成（稳定版）。**

把一条 B站 视频，变成一份"炼真能直接整文件读取"的结构化中转文件（单个 `.md`）。里面包含：

- **核心内容**：字幕（CC优先 → B站 AI字幕 → Whisper ASR 三级兜底）、B站 AI 生成的视频摘要、高能时间点（高光）。
- **观众互动**：弹幕（自动去重 + 过滤废帖）、高赞评论（按赞排序、剔除水帖）、置顶评论（UP主/管理员置顶）。
- **平台信号**：播放/点赞/投币/收藏/分享/评论/弹幕七项计数 + 互动率（赞率/藏率/币率/弹幕密度），视频标签（即熔知关键词）。
- **创作者私有数据**（需以 UP主 身份登录、且是自己的视频）：三秒退出率 / 平均播放时长 / 完播率、播放来源分布；整个账号的概览 / 分区占比 / 粉丝概览快照（`creator_center.md`）。他人视频或未登录时这些字段自动留空，绝不编造。

输出通过文件夹契约交给下游炼真（不直连熔知）。企微群消息监控仍是下一阶段目标，从零调研、不预设方案。

---

## 未来扩展

**企微群消息监控——未来调研。**

- 当前版本先不做，B站摄入流程跑通后再启动调研
- 自动回复列为更后期目标

---

## 学习方向参考：陈云飞案例（2026-08-03 用户指定学习方向）

> 来源：`RESEARCH_CHENYUNFEI_CHAIN.md`（小猫补光灯成功全链路拆解，已录入熔知 doc_0b3a0e2cb705）。AI 提炼、用户拍板写入。

1. **采集"需求信号"（蹲需求自动化）** — 陈云飞的核心方法论是找"用户正在用笨办法凑合"的行为（三条件：有规模 / 有付费意愿 / 现有解法笨）。馏析未来可采集需求信号：评论区用户自述的笨办法、App 一星三星评论的愤怒点/取舍点，归一化后供炼真/凝华使用——把"蹲需求"从人工变成采集流水线的一环。
2. **平台信号归一化（待落地）** — 蓝图"当前重心"已采集播放/点赞/投币/收藏/分享/评论/弹幕 + 互动率，但按 albedo 蓝图设计，未来应归一化成**统一信号包**（互动热度 / 受众契合 / 口碑情感）传给下游，避免下游碰原始平台字段、保持平台无关。当前熔知拿不到播放数据即此环节未落地的表现。

---

## 核心原则

| # | 原则 | 用来拒绝什么 |
|---|------|-------------|
| 1 | **采集优先** | 采集没做完，不做处理管道 |
| 2 | **一对象一适配器** | 不做一个通用框架套所有平台——不同对象可能走完全不同路线 |
| 3 | **先调研再动手** | 不凭感觉选方案，每个平台先广泛搜竞品/开源方案/论文 |
| 4 | **手脚+大脑分离** | 采集用代码（免费），理解用 LLM（按需），不每条消息都烧钱 |
| 5 | **可产品化** | 代码质量和架构要支撑未来"卖出去"的水平，不临时拼凑 |
| 6 | **可独立运行** | 不把"能跑起来"绑死在巨作总管或其他器上——本项目独立仓库、可单独 `run.bat` 启动、有独立输出目录，被串联时通过队列/文件夹契约解耦，不 import 其他器代码 |

---

## 边界

| 不做什么 | 为什么 |
|---------|-------|
| 不做内容创作 | 那是 Rubedo（凝华）的活 |
| 不做知识存储 | 那是 Citrinitas（熔知）的活 |

---

## 验收标准

| # | 怎么算成功 |
|---|-----------|
| 1 | 贴B站链接 → 下载音频 → 生成字幕 → 在UI看到字幕文本 |
| 2 | CC字幕不可用 → 自动用Whisper ASR生成 |
| 3 | 企微监控未来能做——方案调研完成、原型验证通过 |
| 4 | 贴B站链接 → 生成结构化中转文件（字幕+互动数据+创作者信号），炼真可直接整文件读取分析 |

> 企微自动回复列为更后期目标，不在当前范围。
